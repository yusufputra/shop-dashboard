# Hanya base project URL — jangan tambahkan /rest/v1/
NEXT_PUBLIC_SUPABASE_URL=
# Hanya dipakai di server (API proxy + login) — jangan expose ke client
SUPABASE_SERVICE_ROLE_KEY=
# Minimal 32 karakter; dipakai untuk menandatangani cookie sesi JWT
AUTH_SESSION_SECRET=
